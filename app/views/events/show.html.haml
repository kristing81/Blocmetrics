%h3.col.md-6= @event.event_id
%p= @event.event_type
%p= @event.url
%p= @event.ip_address

%br
= link_to "Edit event", edit_event_path(@event), class: 'btn btn-success btn-sm'
= link_to "Delete event", @event, method: :delete, class: 'btn btn-danger btn-sm', data: {confirm: 'Are you sure you want to delete this event?'}
